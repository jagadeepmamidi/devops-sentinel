# Optional operator API (only if you run sentinel serve)
VITE_API_URL=http://localhost:8000

# Optional. The marketing site does not host customer data.
# CLI login uses the Supabase project from the terminal, not these values.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
